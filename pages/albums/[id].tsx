/**
 * This is a free public API: https://jsonplaceholder.typicode.com/albumns/1
 * Requirements:
 * 	- Pre-render data of album at the request time by id.
 * Hint:
 * 	- Using `params` value in the `getServerSideProps` function.
 * 	- Using `getServerSideProps`.
 */

function Album() {
  return <div>{/* add your code here */}</div>
}

export default Album
